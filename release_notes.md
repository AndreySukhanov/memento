Three practices already load-bearing in daily production use but missing from the published method. Each exists because its absence caused a real failure.

## Rule 6 - Correction lenses

Rule 3 protects decisions from being overwritten. Facts had no such protection. When past entries turn out to be factually wrong - wrong person attributed, wrong place, wrong assumption - editing them destroys the record of what you believed and when, and reliably misses occurrences scattered across files.

Now: add a lens at the top of MEMORY.md; every older entry is read through it. One lens corrects dozens of scattered entries without touching them.

Companion Current phase block for volatile state. The failure it prevents: an agent producing forecasts through the frame "contact is broken" four days after contact had resumed - the files were correct, the reading frame was not.

## Rule 7 - Status glyphs

One-character status on TASKS.md sections, MEMORY.md entries, INDEX.md rows. Triage, not decoration: a 30 KB log becomes scannable in one pass, and drift surfaces.

## Rule 8 - Session auto-memory shape

Previously one line in Rule 4 step 6. Now specified: one fact = one file with frontmatter (name, description, type), typed feedback / project / reference / user, plus an index with a why-it-matters clause per row.

Division of labour made explicit: folder files hold task memory (synced by the Rule 4 ritual); auto-memory holds cross-task memory - rules of engagement, stable references, who the user is - and accretes continuously.

## Changes

- skills/task-memory/SKILL.md - five rules to eight
- commands/sync.md - new step 0 and expanded step 6
- templates - Correction lenses + Current phase sections, glyph legends
- CHANGELOG.md added, README.md updated
- plugin.json - 1.1.0 to 1.2.0

No breaking changes: existing folders keep working, new sections are additive.
